# Dependencies
node_modules/
.npm/

# Build / cache outputs
.vercel/
.turbo/
.parcel-cache/
.cache/

# Local env files
.env
.env.local
.env.*.local

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
*.log

# Editor / OS
.DS_Store
Thumbs.db
.idea/
.vscode/

# Firebase — local overrides with real project keys
# Keep only placeholders in version control to avoid secret scanning alerts.
# Use firebase-config.local.js or firebase-config.example.js pattern.
firebase-config.local.js
js/firebase-config.local.js
firebase-config.prod.js

# Optional: if you choose to keep real config only locally and never commit,
# uncomment the next line. By default we keep placeholder version tracked.
# firebase-config.js
